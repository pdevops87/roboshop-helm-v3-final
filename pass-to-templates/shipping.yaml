appName: shipping
cfg_data:
  CART_ENDPOINT: "cart:8080"
  DB_HOST: "mysql-dev.pdevops78.online"
  DB_TYPE:  "mysql"
schema:
  enabled: true

job:
  containers:
    - name: app-user
      file_name: db/app-user.sql
    - name: schema
      file_name: db/schema.sql
    - name: master-data
      file_name: db/master-data.sql